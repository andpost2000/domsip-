// File for vars.
// For example: main colors, paddings and so on.
$c_white = #fff
$c_orange = #ffc107
$c_dark = #424242

// $bp = {
//   mob: 76.8rem,
//   tab: 124rem
// }
// mq($point)
//   @media (max-width: $bp[$point])
//     {block}

// breakpoints adaptiv
$tab = 1240px
$mob = 767px

tab()
  @media (max-width: $tab)
    {block}
mob()
  @media (max-width: $mob)
    {block}
